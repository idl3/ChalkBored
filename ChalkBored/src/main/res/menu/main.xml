<menu xmlns:android="http://schemas.android.com/apk/res/android" android:id="@+id/main_menu">
    <item android:id="@+id/newActBtn"
        android:title="New"
        android:icon="@drawable/newicon"
        android:showAsAction="always" />
    <item android:id="@+id/saveActBtn"
        android:title="Save"
        android:icon="@drawable/saveicon"
        android:showAsAction="always" />
</menu>
